//get that tree setup with key value pairs dynamically determined by user input 

var theTree = {};
//var x = document.getElementById("myText").value;

 function getInput () {
	theTree.height = document.getElementById ("tall").value;
	theTree.character = document.getElementById("char").value;
	
	}

	// create if else for conditions of user interaction

	if ( theTree.height.length   ) {

		for (i = 0; i < theTree.height; i++){

		}


			;
	}

	else  
	{
	  alert("Why you No play the game?");

  /*
	for ( i = 0; i < theTree.height; i++){

	 theTree = theKeys.push[i];

		};

		*/

console.log(theTree);

//then an array will be constructed which will be used to loop throgh
// is the user input of height = to the array length? so input = 20 therefore array equals 20 (0-19)?
// do I just push what the character input is into the array as a string? 


//should I create a global varialbe as userInput to hold a function ?

//setup a for loop to iterate through the newly established array
// and begin looping with a blank space inserted starting at [i = 1]?





//  click event for tree button
//treebutton.addEventListener("click",  ); 

//	document.getElementById("treebutton").addEventListener("click", function(){
	document.getElementById("output").innerHTML = ""
}statu

